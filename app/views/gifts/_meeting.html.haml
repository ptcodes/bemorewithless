- @gift.meeting_comments.includes(:user).order('created_at asc').each do |comment|
  = render partial: 'comments/comment', locals: {comment: comment}
= render partial: 'comments/form', locals: {type_id: 2}
